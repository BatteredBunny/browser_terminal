{
  "name": "browser-terminal-extension",
  "version": "1.0.0",
  "description": "Allows you to open a native shell in the browser",
  "scripts": {
    "dev": "npm run native-manifest install && web-ext run",
    "build": "web-ext build",
    "lint": "web-ext lint",
    "native-manifest": "node native_manifest.js"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^111.0.1",
    "web-ext": "^7.6.1"
  },
  "dependencies": {
    "common-web-styles": "github:ayes-web/common-web-styles",
    "xterm": "^5.1.0",
    "xterm-addon-web-links": "^0.8.0"
  }
}
